{
  "name": "options-analysis",
  "version": "1.4.1",
  "description": "Utilities for analyzing option strategies",
  "main": "lib/index.js",
  "browser": "lib-browser/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "test": "jest",
    "clean": "rm -rf lib && rm -rf lib-browser",
    "build-node": "tsc --build tsconfig.json",
    "build-browser": "tsc --build tsconfig-browser.json",
    "build": "yarn run build-node && yarn run build-browser",
    "add-lib": "git add lib && git add lib-browser",
    "preversion": "yarn test",
    "version": "yarn run build && yarn run add-lib",
    "postversion": "git push && git push --tags"
  },
  "author": "Daniel Imfeld",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/debug": "^0.0.30",
    "@types/jest": "^24.0.23",
    "@types/lodash": "^4.14.76",
    "babel-jest": "^24.9.0",
    "jest": "^24.9.0",
    "jest-extended": "^0.11.2",
    "ts-jest": "^24.2.0",
    "ts-node": "^7",
    "typescript": "^3.7"
  },
  "dependencies": {
    "debug": "^3.1.0",
    "hyperid": "^1.4.1",
    "lodash": "^4.17.4",
    "tda-api": "github:dimfeld/node-tda-api#semver:^2"
  }
}
